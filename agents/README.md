# Agents

Agent implementations will live here in later sprints.

Sprint 0 only establishes the folder boundary:

- Agents receive typed inputs.
- Agents call prompt templates through the prompt registry.
- Agents validate model outputs with Zod schemas.
- Agents return structured results only.
